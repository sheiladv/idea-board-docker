# Etapa única: entorno de desarrollo
FROM node:20-alpine

# Directorio de trabajo
WORKDIR /app

ENV NODE_ENV=development

# Copiamos solo package.json e instalamos dependencias
COPY package*.json ./
RUN npm install --include=dev

# Copiamos el código fuente (si querés podés omitirlo y usar un volumen)
COPY . .

# Exponemos el puerto del servidor de Vite
EXPOSE 5173

# Variables necesarias para hot reload en Docker
ENV HOST=0.0.0.0
ENV CHOKIDAR_USEPOLLING=true
ENV WATCHPACK_POLLING=true

# Permitimos conexiones externas (necesario para hot reload desde tu navegador)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
